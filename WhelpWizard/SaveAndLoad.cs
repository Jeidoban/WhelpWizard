using System;
using PCLStorage;
using Newtonsoft.Json;

namespace WhelpWizard
{
    public class SaveAndLoad
    {
        /*
        IFolder folder = FileSystem.Current.LocalStorage;
        IFile file;

		public SaveAndLoad()
        {
            file = (PCLStorage.IFile)folder.CreateFileAsync("Daisy", CreationCollisionOption.ReplaceExisting);
            file.WriteAllTextAsync("hello world");
        }
        */
    }
}
